{
  "name": "spellnames",
  "collaborators": [
    "Luna Yao <me+github@zznq.buzz>"
  ],
  "description": "A script that transliterates CJK names.",
  "version": "1.0.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ZnqbuZ/SpellNames"
  },
  "scripts": {
    "build": "tsc",
    "copy": "copyfiles -u 1 \"src/**/*.json\" \"build\" && cp LICENSE build && cp README.md build && cp package.json build"
  },
  "dependencies": {
    "@types/node": "^22.13.10"
  }
}
